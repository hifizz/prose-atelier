---
title: Example article
date: 9 May, 2026
---

import { ArticleLayout, Demo } from "@/app/_article";
import { TickingDot, SpeedDemo } from "./demos";

export const tagline = (
  <>
    A working demo of the article template — editorial typography, a left-rail
    TOC, and embedded interactive demos, all driven by a single{" "}
    <code>page.mdx</code> file.
  </>
);

export default function MDXLayout({ children }) {
  return (
    <ArticleLayout
      meta={{
        ...meta,
        tagline,
        toc: { label: "Sections" },
        back: { href: "/", label: "← /article-template" },
      }}
    >
      {children}
    </ArticleLayout>
  );
}

This page is one MDX file. The frontmatter at the top fills the header (title
and date). The `tagline` JSX export fills the subtitle. Everything below this
paragraph is regular prose that flows down a single ~36rem column, with the
table of contents pinned to the left rail at large viewports.

The point of the template is that you get to write *prose*, not JSX. Headings
are `## like this`. Links are `[like this](https://example.com)`. Code lives in
fenced blocks. The only place you reach for components is when embedding a live
demo inside a section.

## How to add a new article

Create `app/<slug>/page.mdx`. Mirror the frontmatter + default-export pattern
from this file. If your article has interactive bits, put them in a sibling
`demos.tsx` marked `"use client"` and import them at the top of the MDX file.

```mdx
---
title: My article
date: 9 May, 2026
---

import { ArticleLayout, Demo } from "@/app/_article";
import { MyDemo } from "./demos";

export const tagline = <>One sentence subtitle.</>;
export default ({ children }) => (
  <ArticleLayout meta={{ ...meta, tagline }}>{children}</ArticleLayout>
);

Body prose...

## A section heading

<Demo theme="dark" caption={<>Caption text.</>} tag="optional-tag">
  <MyDemo />
</Demo>
```

### Optional layout knobs

You can override two things via `meta`:

- `meta.toc.label` — change the sidebar header (default: `Contents`). This
  page uses `Sections`.
- `meta.back` — `{ href, label }` for the back link in the top-left. This
  page links to `/` with the label *← /article-template*.

Pass `meta.toc: false` if you want to disable the TOC entirely.

## Section dividers come for free

Every `## h2` becomes a horizontal divider with the heading text inlined on the
line, *and* an entry in the left TOC sidebar. Active-section tracking is
handled by an IntersectionObserver in `app/_article/article-toc.tsx`, so it
just works as you scroll. There's nothing to register.

The TOC is hidden under 1100px. The article column itself stays centered at
all widths.

### h3 sub-headings

`### Like this` rendered without a horizontal line — they live inside an h2
section as a quieter break. h3s do not appear in the TOC.

## Lists, quotes, emphasis

The body styles cover everything you'd expect from prose. Unordered lists:

- **Bullet** — bold runs in the same color as body text but a heavier weight.
- *Italics* are rendered in the serif italic (`Newsreader`), one notch larger
  than the surrounding sans, with a slight color bump. Used for emphasis the
  way an editor would.
- Inline `code` lives in a soft gray pill so it doesn't fight with the prose.

Ordered lists work the same:

1. First.
2. Second, with a `code-snippet`.
3. Third, with [an external link](https://example.com).

Block quotes pick up the serif italic too:

> Typography is the design with which the medium of the printed word lives.
> Look at it from the writer's side: nobody wants their words to scream.

The whole prose color is `#111`, not gray. Soft gray is reserved for
metadata: the date in the header, the section labels on the dividers, the
caption text under demos.

## Embedding a live demo

The `<Demo>` primitive wraps your component in a dark (or light) card with a
caption underneath:

<Demo
  theme="dark"
  caption={<><b>Ticking dot.</b> A trivially simple SVG that updates every animation frame, just so this section has something moving in it.</>}
  tag="rAF"
>
  <TickingDot />
</Demo>

## Demos with external controls

Sometimes the demo needs interactive controls that should sit *outside* the
dark frame — toggle chips, sliders, segmented controls. For that, skip
`<Demo>` and compose the lower-level primitives yourself. The component
renders its own `art-demo-frame` and the chips below it; the MDX wraps the
whole thing in `art-demo`:

```tsx
// inside SpeedDemo (use client)
return (
  <>
    <div className="art-demo-frame">{/* ... */}</div>
    <div style={{ display: "flex", justifyContent: "center" }}>
      {/* chip row here */}
    </div>
  </>
);
```

```mdx
<div className="art-demo">
  <SpeedDemo />
  <div className="art-demo-caption">
    <span><b>Speed control.</b> Click a chip to retime the dot.</span>
    <span className="art-demo-tag">controls</span>
  </div>
</div>
```

<div className="art-demo">
  <SpeedDemo />
  <div className="art-demo-caption">
    <span><b>Speed control.</b> Click a chip to retime the dot. The chips sit
    outside the dark frame so they read as part of the page, not part of the
    demo.</span>
    <span className="art-demo-tag">controls</span>
  </div>
</div>

## Fenced code

Code blocks are tokenized at build time by Shiki (vitesse-light theme), wired
in via `app/_codeblock`. The whole config — themes, transformers, language
aliases — lives in one file (`rehype-shiki.mjs`) so customizing is editing
that one file, not threading options through layers.

```ts
import { ArticleLayout } from "@/app/_article";

export const meta = {
  title: "Hello",
  date: "1 Jan, 2026",
};
```

### Authoring annotations

Every transformer is opt-in: nothing happens until you put a comment marker
in the code (or a meta string after the language). Diff markers:

```ts
const stable = "still here";
const removed = "going away"; // [!code --]
const added = "brand new"; // [!code ++]
```

Line highlight via the meta string:

```ts {2,4}
const a = 1;
const b = 2; // ← highlighted via {2,4}
const c = 3;
const d = 4; // ← highlighted via {2,4}
```

Word highlight via the meta string. Every occurrence of `useState` gets a
soft yellow:

```tsx /useState/
import { useState } from "react";
function Counter() {
  const [n, setN] = useState(0);
  return <button onClick={() => setN(n + 1)}>{n}</button>;
}
```

Focus dims everything except the marked lines. Hover the block to see the
dimmed lines pop back:

```ts
function quickSort(arr) {
  if (arr.length <= 1) return arr;
  const pivot = arr[0]; // [!code focus]
  const left = arr.slice(1).filter((x) => x < pivot); // [!code focus]
  const right = arr.slice(1).filter((x) => x >= pivot); // [!code focus]
  return [...quickSort(left), pivot, ...quickSort(right)];
}
```

Error / warning level annotations get a soft red / amber strip:

```ts
fetch("/api/things")
  .then((r) => r.json())
  .then((data) => {
    console.log(data.naem); // [!code error] typo
    cache.set(KEY, data); // [!code warning] no TTL
  });
```

To customize: edit `app/_codeblock/rehype-shiki.mjs` (themes, transformer
list, language aliases all live there as plain JS data). To copy the whole
setup to a fresh project, see `app/_codeblock/README.md` — about 30 seconds
of work.

---

That's it. Three files per article: `page.mdx`, optional `demos.tsx` for
client-side bits, and any article-specific CSS you need (rare). The shared
template lives entirely under `app/_article/` (typography + TOC) and
`app/_codeblock/` (Shiki-powered fenced code).
